"""PillowFort development tooling package.

Keep this module free of command execution and expensive imports. Its eventual
job is only to expose deliberately public package metadata, such as a tooling
version, if the build layer needs it.
"""
